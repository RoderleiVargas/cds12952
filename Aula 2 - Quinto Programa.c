#include <stdio.h>

int main()
{
    int unidades = 4;
    float preco = 5.5;
    float total = unidades * preco;
    printf("Total = %f\n", total);
    printf("Total = %f\n", unidades * preco);
    return 0;
}

