Estrutura basica de um programa

#include <stdio.h> // cabeçalho necessário para utilizar o printf e scanf

// Declaração da função main()
int main ()
{

	//corpo do programa
	//fim do programa
	return 0;
}

imoressão na tela
	printf
	puts
	Diferenças entre printf e puts 
		puts essa função coloca um \n no final da string NÃO aceita variáveis - puts("Hello World; %d,x) não será aceito pelo puts


Receber dados dos usuários (scanf)

int x; // Mais de um caracter é chamado é de string -> en C mais de uma palavra é um vetor de caracteres
scanf(%d, x); // Quando é uma palavra usa o %s e não coloca o & na frente do nome
printf("\n = %d\n, x);


char x[100];
scanf("%s", x);
printf("\nx = %s\n",x);


Existe a função fgets() para ler caracteres


fgrs(x, 100, stdin);


String -> Texto -> letras e caracteres
printf("nx %c%c\n", x[1], x[3]); irá imprimir a primeira e a terceira letra de uma palavra

Operadores

1 - Operadores aritméticos
	+ - * / % (o percent retorna o resto da divisão)
2 - 


%d = inteiro = int
%f = numero com virgula - float
%c = unico caracter = char
%s = string caracter = char colocar o tamanho entre colchetes

& = utilizado somente quando são nros ou um unico caracter

\t = tab
\n linha em branco

#include <stdio.h>

float cotacao_dolar, valor_em_dolares, conversao;

int main()
{
    printf("\nDigite a cotação do Dolar: ");
    scanf("%f", &cotacao_dolar);
    
    printf("\nDigite o valor em dolcares: ");
    scanf("%f", &valor_em_dolares);
    
    conversao = cotacao_dolar * valor_em_dolares;
    
    printf ("\nO valor convertido é: %f", conversao);
    
    return 0;
}

}


/******************************************************************************
                            Online C Compiler.
                Code, Compile, Run and Debug C program online.
Write your code in this editor and press "Run" button to compile and execute it.
*******************************************************************************/

#include <stdio.h>

float n1, n2, n3, n4, q1, q2, q3, q4, soma;

int main()
{
    printf("\nDigite o primeiro nro: ");
    scanf("%f", &n1);
    
    printf("\nDigite o segundo nro: ");
    scanf("%f", &n2);
    
    printf("\nDigite o terceiro nro: ");
    scanf("%f", &n3);
    
    printf("\nDigite o quarto nro: ");
    scanf("%f", &n4);
    
    q1 = n1 * n1;
    
    q2 = n2 * n2;
    
    q3 = n3 * n3;
    
    q4 = n4 * n4;
    
    soma = q1 + q2 + q3 + q4;
    
    printf("\nA soma total é : %f ", soma);
    
    return 0;
}

Terceiro programa com erro *********************

/******************************************************************************
                            Online C Compiler.
                Code, Compile, Run and Debug C program online.
Write your code in this editor and press "Run" button to compile and execute it.
*******************************************************************************/

#include <stdio.h>

int main()
{
char nome_vendedor, codigo_peca;

float preco_unitario_peca, qtda_peca, comissao;

    printf("\nDigite o nome do vendedor: ");
    scanf ("%c", &nome_vendedor);
    
    printf("\nDigite o codigo da peça: ");
    scanf("%c", &codigo_peca);
    
    printf("\nDigite o preço unitário da peça: ");
    scanf("%f", &preco_unitario_peca);
    
    printf("\nDigite a quantidade de peças: ");
    scanf("%f", &qtda_peca);
    
    comissao = (preco_unitario_peca * qtda_peca) * 5 /100;
    
    printf("\nA comissão é : %f ", comissao);
    
    return 0;
}



PROGRAMA CORRIGIDO PARA LEO



#include <stdio.h>

int main()
{
char nome_vendedor[50], codigo_peca[10];

float preco_unitario_peca, qtda_peca, comissao;

    printf("\nDigite o nome do vendedor: ");
    scanf ("%s", nome_vendedor);
    
    printf("\nDigite o codigo da peça: ");
    scanf("%s", codigo_peca);
    
    printf("\nDigite o preço unitário da peça: ");
    scanf("%f", &preco_unitario_peca);
    
    printf("\nDigite a quantidade de peças: ");
    scanf("%f", &qtda_peca);
    
    comissao = (preco_unitario_peca * qtda_peca) * 5 /100;
    
    printf("\nA comissão é : %f ", comissao);
    
    return 0;
}



Exercio de aula

#include <stdio.h>

char nome[50];
int idade;

int main()
{
    printf("\nDigite seu nome: ");
    scanf("%s", nome);
    
    printf("\nDigite sua idade: ");
    scanf("%d", &idade);
    
    printf ("\nSeu nome é %s e sua idade é %d anos", nome, idade);
    
    return 0;
}

Para descobrir se um nro é par ou impar

int a;
scanf("%d , &a);
if (a % 2 == 0)
{
	puts("o nro é par");
}
else
{
	puts("o nro é impar");
}

SWITCH

int opcao;
puts("1. Recarregar");
puts("2. Consultar saldo");
puts("9. Falar com atendente");
scanf(%d, &opcao);

switch(opcao)
{
	case 1;
		puts("Recarregar");
		// codigo para recarregar o celular
		break;
	case 2;
		puts("consultar saldo");
		// codigo para consultar o sadlo
		break;
	case 3;
		puts("falar com atendente");
		// codigo para falar com atendente
		break;
}


Exercicio AULA 2

#include <stdio.h>

float peso, excesso = 0 , multa = 0;

int main()
{
    printf("\nDigite o peso: ");
    scanf("%f", &peso);

    if(peso > 50) 
    {
        excesso = peso -50;
        multa = excesso * 4;
    }
printf("Peso: %2.2f\n", peso);
printf("Excesso:%2.2f\n", excesso);
printf("Multa: %2.2f\n", multa);

return 0;
    
}


Exercicio 2

#include <stdio.h>

int codigo_funcionario;
float numero_horas, salario, extra, salario_total;

int main()
{
    printf("\nDigite o codigo do funcionário: ");
    scanf("%d", &codigo_funcionario);
    
    printf("\nDigite o número de horas: ");
    scanf("%f", &numero_horas);
    
    salario = numero_horas * 10;

    if(numero_horas > 50) 
    {
        extra = (numero_horas - 50) * 20;
        salario_total = salario + extra;
        
    }
printf("Salario: %2.2f\n", salario);
printf("Salario Excedente:%2.2f\n", extra);
printf("Salario Total: %2.2f\n", salario_total);

return 0;
    
}

------------

Exercicio 3

#include <stdio.h>

int n1, n2, n3, n4, q1, q2, q3, q4;

int main()
{
    printf("\nDigite o 1º nro: ");
    scanf("%d", &n1);
    
    printf("\nDigite o 2º nro: ");
    scanf("%d", &n2);
    
    printf("\nDigite o 3º nro: ");
    scanf("%d", &n3);
    
    printf("\nDigite o 4º nro: ");
    scanf("%d", &n4);
    
    q1 = n1 * n1;
    q2 = n2 * n2;
    q3 = n3 * n3;
    q4 = n4 * n4;
    
    if(q3 >= 1000)
    {
        printf("\nO valor de q3 é: %d", q3);
        return 0;
    }
        
printf ("\nO valor de n1 é: %d e o quadrado é %d", n1, q1);
printf ("\nO valor de n2 é: %d e o quadrado é %d", n2, q2);
printf ("\nO valor de n2 é: %d e o quadrado é %d", n3, q3);
printf ("\nO valor de n2 é: %d e o quadrado é %d", n3, q3);

return 0;
}

--------------------

Quarto Exercicio

#include <stdio.h>

int numero = 0;

int main()
{
printf("\nDigite o nro: ");
scanf("%d", &numero);

if (numero % 2 == 0)
{
	puts("o nro é par");
}
else
{
	puts("o nro é impar");
}

if (numero > 0)
{
	puts("o nro é positivo");
}
else
{
	puts("o nro é negativo");
}

}